{
  "name": "oskm",
  "description": "Knowledge map of open source",
  "scripts": {
    "prebuild": "mkdir -p Public/",
    "build": "npm run build:default -- --no-open",
    "build:default": "markmap Content/oskm.md --output Public/index.html",
    "dev": "npm run build:default -- --watch"
  },
  "dependencies": {
    "markmap-cli": "^0.14.3",
    "markmap-common": "^0.14.2"
  },
  "license": "CC-BY-4.0"
}
